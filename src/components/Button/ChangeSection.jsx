import React from 'react'
import Education from '../Education_Experience/Education';
import { useState } from 'react';
const { education } = CV;

export default function ChangeSection(section) {
    const [showSection, setshowSection] = useState(educationSection);
    setshowSection(section)
  return (
      
}

